<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<meta
			name="description"
			content="NRVA — electronic music for the fractured mind. Listen to the latest single, browse releases and remixes."
		/>
		<meta name="theme-color" content="#120c10" />
		<title>Home — NRVA</title>
		
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			rel="stylesheet"
			href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css"
		/>
		<link
			href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;900&family=DM+Mono:wght@300;400;500&display=swap"
			rel="stylesheet"
		/>
		<script src="https://unpkg.com/@studio-freight/lenis@1.0.42/dist/lenis.min.js"></script>
		<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
		<script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/3.2.2/anime.min.js"></script>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
/* Hallmark · genre: atmospheric · macrostructure: Marquee Hero · theme: custom · vibe: "late-night, neon, immersive club, fractured"
 * paper: oklch(14% 0.015 350) · accent: oklch(60% 0.20 350)
 * display: Inter · body: Inter · axes: dark / geometric-sans / chromatic-other (electric magenta)
 * studied: no · context: explicit
 */

:root {
	/* Palette */
	--color-paper: oklch(14% 0.015 20);
	--color-paper-2: oklch(18% 0.018 20);
	--color-ink: oklch(96% 0.010 20);
	--color-ink-2: oklch(82% 0.012 20);
	--color-rule: oklch(28% 0.015 20);
	--color-muted: oklch(56% 0.015 20);
	--color-accent: oklch(58% 0.22 25); /* True Red */
	--color-accent-ink: oklch(96% 0.010 20);
	--color-focus: oklch(62% 0.24 25);

	/* Typography */
	--font-display: 'Inter', sans-serif;
	--font-body: 'Inter', sans-serif;
	--font-mono: 'DM Mono', monospace;

	--text-xs: 0.75rem;
	--text-sm: 0.875rem;
	--text-md: 1.125rem;
	--text-lg: 1.375rem;
	--text-xl: 1.75rem;
	--text-2xl: 2.25rem;
	--text-3xl: 3rem;
	--text-hero: clamp(3rem, 8vw, 8rem);

	/* Spacing */
	--space-3xs: 0.25rem;
	--space-2xs: 0.5rem;
	--space-xs: 0.75rem;
	--space-sm: 1rem;
	--space-md: 1.5rem;
	--space-lg: 2rem;
	--space-xl: 3rem;
	--space-2xl: 4.5rem;
	--space-3xl: 7rem;
	--space-4xl: 10rem;

	/* Motion & Shape */
	--ease-out: cubic-bezier(0.16, 1, 0.3, 1);
	--dur-short: 220ms;
	--dur-medium: 400ms;
	--dur-long: 800ms;
	--radius-card: 16px;
	--radius-pill: 9999px;
}

*, *::before, *::after {
	box-sizing: border-box;
}

html, body {
	margin: 0;
	padding: 0;
	width: 100%;
	min-height: 100%;
	background-color: var(--color-paper);
	color: var(--color-ink);
	font-family: var(--font-body);
	line-height: 1.5;
	overflow-x: clip; /* Required for Hallmark responsiveness */
	-webkit-font-smoothing: antialiased;
}

/* Base typography */
h1, h2, h3, h4, h5, h6 {
	font-family: var(--font-display);
	font-weight: 600;
	margin: 0;
	line-height: 1.1;
	letter-spacing: -0.03em;
	font-style: normal; /* Hallmark: Italics in headers banned */
	overflow-wrap: anywhere;
	min-width: 0;
}

p {
	margin: 0;
	color: var(--color-ink-2);
}

a {
	color: inherit;
	text-decoration: none;
	transition: color var(--dur-short) var(--ease-out);
}

a:hover {
	color: var(--color-accent);
}

a:focus-visible, button:focus-visible {
	outline: 2px solid var(--color-focus);
	outline-offset: 4px;
	border-radius: 4px;
}

/* Atmospheric blooms */
.atmospheric-bloom {
	position: fixed;
	width: 60vw;
	height: 60vw;
	border-radius: 50%;
	background: radial-gradient(circle at center, var(--color-accent) 0%, transparent 70%);
	opacity: 0.08;
	pointer-events: none;
	z-index: -1;
	filter: blur(80px);
}

.bloom-top-right {
	top: -20vw;
	right: -10vw;
}

.bloom-bottom-left {
	bottom: -20vw;
	left: -20vw;
}

/* Global utility classes */
.container {
	max-width: 1200px;
	margin: 0 auto;
	padding-inline: clamp(1rem, 4vw, 1.5rem);
}

.reveal-on-scroll {
	opacity: 0;
	transform: translateY(20px);
	transition: opacity var(--dur-long) var(--ease-out), transform var(--dur-long) var(--ease-out);
}

.reveal-on-scroll.is-visible {
	opacity: 1;
	transform: translateY(0);
}

@media (prefers-reduced-motion: reduce) {
	* {
		transition-duration: 0.01ms !important;
		animation-duration: 0.01ms !important;
	}
}
</style>
